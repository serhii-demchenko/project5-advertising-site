<ul class="category-list">
    <li id="{{id}}" class="carousel slide w-100 category" data-ride="carousel">
        <div class="category__nav">
            {{!-- <button class="previous-card">
                <span class="material-icons">
                    keyboard_arrow_left
                </span>
            </button> --}}
            <h2 class="category-discription">{{ category}}</h2>
            <button class="look-more">Дивитися всі</button>
            {{!-- <button class="next-card">
                <span class="material-icons">
                    keyboard_arrow_right
                </span>
            </button> --}}
        </div>
        {{!-- <ol class="carousel-indicators carousel-indicators-numbers">
            {{#each array}}
            {{#if @first}}
            <li data-target="#{{../id}}" data-slide-to="{{@index}}" class="active"></li>
            {{else}}
            <li data-target="#{{../id}}" data-slide-to="{{@index}}"></li>
            {{/if}}
            {{/each}}
        </ol> --}}

        <div class="carousel-inner w-100" role="listbox">
            {{!-- Тут меняем разметку карточки, если нужно --}}
            {{#each array}}
            {{#if @first}}
            <div class="carousel-item carousel-item__pos active">
                <div class="col-md-3">
                    {{> card}}
                </div>
            </div>
            {{else}}
            <div class="carousel-item carousel-item__pos">
                <div class="col-md-3">
                    {{> card}}
                </div>
            </div>
            {{/if}}
            {{/each}}
        </div>

        <div class="category__nav">
            <a class="carousel-control-prev w-auto previous-card" href="#{{id}}" role="button" data-slide="prev">
                <span class="material-icons" aria-hidden="true">keyboard_arrow_left</span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next w-auto next-card" href="#{{id}}" role="button" data-slide="next">
                <span class="material-icons" aria-hidden="true">keyboard_arrow_right</span>
                <span class="sr-only">Next</span>
            </a>

        </div>
    </li>
</ul>